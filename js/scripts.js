// Business Logic
var pingPong = function(number1, number2){
  var ping = "ping";  // scaleable: in case dev/manager/user wants to change output
  var pong = "pong";  // same as above
  var num1 = number1; // Scaleable: in case dev/manager/user wants to change input
  var num2 = number2; // same as above

  if (isNaN(num1) || isNaN(num2)) {  // error handling for words and/or empty input
    return "error message";  // define 'error message' in Interface Logic section
  } else {

  }

  // if (num1 %) && (num2 === 5) {
  //   return ("ping"
  // } else if (number2 === 5) {
  //   return "pong"
  // } else if (true) {
  //
  // }

};










// User Interface Logic
